spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/sport_goods?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=UTC&useSSL=false
      username: root
      password: 135223
      max-active: 20
      max-wait: 5000
      initial-size: 1
      filters: stat,log4j,wall
      validation-query: SELECT 'X'   #验证连接
      enable: true
      # stat-view-servlet:
      #   enabled: true
      #   login-username: root
      #   login-password: 135223
      #   url-pattern: /druid/*



server:
  port: 8888